#include <iostream>
#include <vector>
#include <hash_map>

using std::vector;
using std::cin;

int main() {
    int n;
    cin >> n;
    vector<int> colors(n+1); // ?
    map<int, vector<int>> edges;
    for (int i = 1; i<=n; i++) {
        cin >> colors[i];
    }
    int node;
    for (int i = 1; i<= n-1; i++) {
        
    }
}